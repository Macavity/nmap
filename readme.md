# Nmap Console Prototype

1. `composer install`
2. Enter database details in .env based on .env.example
3. `php artisan db:migrate` 
4. `php artisan db:seed` for a task example
5. Run example task with `php artisan nmap:scan 1`